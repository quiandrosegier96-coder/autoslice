[project]
name = "autoslice-backend"
version = "1.3.22"
description = "AutoSlice backend — Bambu 3MF to Anycubic 3MF conversion engine"
requires-python = ">=3.11"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py311"
